export class Team {
  nome: string;
  color: string;
  logo: String;

  constructor(nome: string, color: string, logo: String) {
    this.nome = nome;
    this.color = color;
    this.logo = logo;
  }
}
